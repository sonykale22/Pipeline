pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello'
            }
        }
		stage('Test') {
            steps {
                sleep60
            }
        }
		stage('Deploy') {
            steps {
                echo 'Hello2'
            }
        }
		
		
		
    }
}
